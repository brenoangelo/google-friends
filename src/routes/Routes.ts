// Routes
// Main
export const HOME:string = "/"
export const NEWROOM:string = "/rooms/new"
export const ROOM:string = "/rooms/:id"

    // Room Page Routes
    export const CHAT:string = `${ROOM}/`
    export const BOOKS:string = `${ROOM}/books`
    export const MOVIES:string = `${ROOM}/movies`
    export const JOBS:string = `${ROOM}/jobs`